<div class="max-w-md mx-auto w-full">
  <%= link_to "← #{@pet.name}のページに戻る", pet_path(@pet), class: "link text-sm mb-4 inline-block" %>
  <h1 class="text-2xl font-bold mb-6"><%= @pet.name %>の情報を編集</h1>

  <%= render "form", pet: @pet, submit_label: "更新する" %>
</div>
